import React from 'react'
import './Screen.scss'

const Screen = ({value}) => {
    return (
        <div className="con-screen">
            <p>{value}</p>
        </div>
    )
}

export default Screen
